<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Login">
    <resultMap id="BaseResultMap" type="com.elk.elkweb.entity.User">
        <id column="sno" jdbcType="INTEGER" property="sno" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
    </resultMap>

    <!--登陆验证-->
    <select id="LoginMapper" parameterType="User" resultType="User">
        select * from  T_USER_INFO where username=#{USER_NAME} and password=#{USER_PSD}
    </select>

</mapper>
